---
import SectionTitle from "../components/SectionTitle.astro";
import { Icon } from "astro-icon";
import Button from "../components/Button.astro";

const socialMedia = [
  { icon: "mdi:instagram", href: "https://www.instagram.com/hector___reyes/" },
  { icon: "mdi:twitter", href: "https://twitter.com/hector_reyes_" },
  { icon: "mdi:linkedin", href: "https://www.linkedin.com/in/hectorreyesdev/" },
];
---

<section class="w-full p-4 mb-8">
  <SectionTitle
    headline="Encuentrame"
    colorHeadline="aquí"
    subtitle="contacto"
  />
  <div class="w-full flex gap-8 items-center justify-center">
    {
      socialMedia.map(({ icon, href }) => (
        <Button type="icon" href={href} target="_blank">
          <Icon name={icon} class="text-white w-8" />
        </Button>
      ))
    }
  </div>
</section>
