---
import { Image } from "astro:assets";
import { Countdown } from "./Counter";

import NamesDesktop from "@assets/wedding/names-desktop.png";
import NamesMobile from "@assets/wedding/names-mobile.png";
import headerImage from "@assets/wedding/header-image.png";
import Button from "./Button.astro";

const QUOTE =
  "“Como llama divina es el fuego ardiente del amor. Ni las muchas aguas pueden apagarlo, ni los ríos pueden extinguirlo” - Cantares 8:6-7";
const SUBTITLE = "Nos casamos";
const TEXT =
  "Te invitamos a celebrar con nosotros este gran momento de nuestras vidas, donde el amor se convierte en nuestro más bello vínculo y el comienzo de una eterna aventura juntos.";
---

<Image
  src={headerImage}
  alt="Imagen"
  class="w-full absolute opacity-80 z-50 small"
/>

<section class="mt-20 md:mt-40 flex flex-col items-center justify-center gap-8">
  <span class="w-9/12 md:w-6/12 italic text-center text-balance">{QUOTE}</span>

  <Image
    src={NamesDesktop}
    alt="Carol y Héctor"
    class="w-10/12 hidden md:block appear"
  />
  <Image
    src={NamesMobile}
    alt="Carol y Héctor"
    class="w-full block md:hidden appear"
  />

  <div class="mt-4 flex flex-col items-center justify-center gap-4">
    <span class="subtitle text-center">{SUBTITLE}</span>
    <Countdown client:load />

    <div class="w-full md:w-2/3 md:text-center md:text-pretty">
      <p class="mb-8">
        {TEXT}
      </p>
      <Button
        url="/boda#ConfirmAssistance"
        target="_self"
        label="Confirmar asistencia"
      />
    </div>
  </div>
</section>
